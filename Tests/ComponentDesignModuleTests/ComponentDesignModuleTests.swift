import XCTest
@testable import ComponentDesignModule

final class ComponentDesignModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ComponentDesignModule().text, "Hello, World!")
    }
}
